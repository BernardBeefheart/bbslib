# ======================================================================
# chicken-config.mk
# ======================================================================

CSC = csc -c -debug-level 0 -verbose  -I.
CSLD = csc

%.o: %.scm
	$(CSC) $<

